package org.example.units;

public abstract class Range extends BaseHero{
    public Range(float hp, int x, int y, int attack, int[] dmg, int def, String name) {
        super(hp, x, y, attack, dmg, def, name);
    }
}
